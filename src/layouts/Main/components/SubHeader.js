import React from 'react';
import './MainMenu/Mainmenu.scss';

const SubHeader = ({ subMenu }) => <div className="sub-menu">{subMenu}</div>;

SubHeader.propTypes = {};

SubHeader.defaultProps = {};

export default SubHeader;
